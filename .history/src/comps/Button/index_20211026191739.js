import styled from 'styled-components';
import React from 'react';

const ButtonCont = styled.div`
    margin-top:${props=>props.margi}
`;


const Button = () => {
    return (


    );
}

export default Button;