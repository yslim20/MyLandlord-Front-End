import styled from 'styled-components';
import React from 'react';

const FormCont = styled.div`
    max-width: 650px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
`;

const LabelFor = styled.l

const LongInForm = () => {
    return (
        <FormCont>

        </FormCont>
    );
}